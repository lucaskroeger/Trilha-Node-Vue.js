class Saida {
    constructor({ id, produto_id, quantidade, data }) {
        this.id = id;
        this.produto_id = produto_id;
        this.quantidade = quantidade;
        this.data = data;
    }
}

module.exports = Saida;
